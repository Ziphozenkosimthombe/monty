#include "monty.h"

/**
 * opcode_nop - do not do anything
 * @stack: doesn't needed
 * @amount: nothing
 *
 * Return: nothing
 */
void opcode_nop(stack_t **stack, unsigned int amount)
{
	(void) stack;
	(void) amount;
}
