#include "monty.h"

/**
 * opcode_stack - the top of the stack will be printed
 * @stack: head of stack
 * @amount: line counter
 *
 * Return: nothing
 */
void opcode_stack(stack_t **stack, unsigned int amount)
{
	(void)stack;
	(void)amount;
	bus.lifi = 0;
}
